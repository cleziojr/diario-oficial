# Prefixo opcional para chamadas à API (ex.: produção atrás do mesmo host).
# Em desenvolvimento, deixe vazio: o Vite faz proxy de /api → backend (vite.config).
# VITE_API_BASE=

# Onde o proxy aponta (só dev). Padrão: http://127.0.0.1:8080
# VITE_PROXY_API=http://127.0.0.1:8080
